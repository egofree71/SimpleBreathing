# C# reference files

These files are kept only as a migration reference during the GDScript port.
They use `.txt` extensions so Godot does not compile them as C# scripts.

The active implementation should use the GDScript files in `scripts/`.
